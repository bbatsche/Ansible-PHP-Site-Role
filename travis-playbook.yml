---
- hosts: all

  vars_files:
    - defaults/main.yml
    - vars/main.yml

  vars:
    php_version: "7.1.10"

  roles:
    - role: bbatsche.Nginx
      nginx_configs: [ "php.conf" ]

  tasks:
    - include: tasks/main.yml

  handlers:
    - include: handlers/main.yml
