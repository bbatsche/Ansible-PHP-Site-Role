---
phpenv_root: /usr/local/phpenv

pear_dirs:
  cache_dir:    /tmp/phpenv/{{ php_version }}/pear/cache
  download_dir: /tmp/phpenv/{{ php_version }}/pear/download
  temp_dir:     /tmp/phpenv/{{ php_version }}/pear

log_dir:      "{{ phpenv_root }}/versions/{{ php_version }}/var/log"
composer_dir: "{{ phpenv_root }}/versions/{{ php_version }}/composer"
